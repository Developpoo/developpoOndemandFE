export type Auth = {
    idLingua: number | null,
    token: string | null,
    nome: string | null,
    idUserRole: number | null,
    idUserStatus: number | null,
    ability: number[] | null,
    idUserClient: number | null
}